@model IEnumerable<AdminLteMvc.Departamento>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLteLayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CODIGO_DEPARTAMENTO }) |
            @Html.ActionLink("Details", "Details", new { id=item.CODIGO_DEPARTAMENTO }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CODIGO_DEPARTAMENTO })
        </td>
    </tr>
}

</table>
